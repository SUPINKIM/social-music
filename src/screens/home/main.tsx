import React from 'react';
import { SafeAreaView } from 'react-native';

import Appbar from '@components/appbar';

const Main = () => {
    return (
        <SafeAreaView>
            <Appbar />
        </SafeAreaView>
    );
};

export default Main;
