import { SafeAreaView } from 'react-native';
import { Text } from 'react-native-paper';

const MyPage = () => {
    return (
        <SafeAreaView>
            <Text>my-page</Text>
        </SafeAreaView>
    );
};

export default MyPage;
